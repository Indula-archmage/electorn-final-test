{
	"name": "test-three",
	"version": "1.0.0",
	"description": "",
	"main": "main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "electron .",
		"build": "electron-builder build --mac --win --publish never",
		"dev": "electron-builder --win --mac -p always"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"electron": "^18.2.2",
		"electron-builder": "^23.0.3"
	},
	"dependencies": {
		"electron-updater": "^5.0.1"
	}
}
